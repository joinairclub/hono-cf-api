# Recommended local development values (copy to .env.local).
# db:* scripts load this file through DOTENV_CONFIG_PATH=.env.local.
MIGRATION_DATABASE_URL=postgresql://postgres:postgres@localhost:5433/hono_cf
# This maps to binding name HYPERDRIVE in wrangler.jsonc.
CLOUDFLARE_HYPERDRIVE_LOCAL_CONNECTION_STRING_HYPERDRIVE=postgresql://postgres:postgres@localhost:5433/hono_cf

# Production migrations should come from CI/secret manager (not local files):
# MIGRATION_DATABASE_URL=postgresql://user:password@host:5432/databasename
